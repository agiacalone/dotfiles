# Fedora/Kinoite-specific zsh configuration

# Load Nord theme for ls colors
test -r ~/.dir_colors && eval $(dircolors ~/.dir_colors)

# Use terminal for GPG
export GPG_TTY=$(tty)

# Flatpak binaries
export PATH="/var/lib/flatpak/exports/share:$PATH"
