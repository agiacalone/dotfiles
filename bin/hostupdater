#!/bin/bash

sudo python3 ~/git/hosts/updateHostsFile.py --auto -r -e fakenews

# Flush DNS cache
case "$(uname -s)" in
  Darwin)
    sudo dscacheutil -flushcache
    sudo killall -HUP mDNSResponder
    ;;
  Linux)
    sudo systemd-resolve --flush-caches
    ;;
esac
